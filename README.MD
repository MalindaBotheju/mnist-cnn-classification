# MNIST CNN Classifier (PyTorch)

This project trains a Convolutional Neural Network (CNN) to classify handwritten digits from the MNIST dataset using PyTorch.  
The model achieves around **99% accuracy** on the test set.

---

## 🚀 Features
- Loads and preprocesses MNIST dataset
- Builds a CNN using PyTorch
- Trains for 5 epochs
- Evaluates test accuracy
- Saves the trained model (`mnist_cnn.pt`)

---

## 🛠 Requirements
Install dependencies using:

```bash
pip install -r requirements.txt
